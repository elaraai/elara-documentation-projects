// © Copyright 2018- 2022 - Elara AI Pty Ltd ACN 627 124 903
//
// Licensed under the EDK Licence version LICENSE-1.04.
//
// You may only use this file in accordance with the terms and conditions contained in EDK
// Licence - Version LICENSE-1.04. The full version of the EDK Licence is available at:
// https://elaraai.github.io/docs/license

import { ELARASchema } from "../schema/ELARASchema";
import { Grant } from "../admin/Grant";
import { UserDefinition } from "../admin/User";
import { EnvironmentDefinition } from "../admin/Environment";
/**
 * A {@link AdminPluginDefinition} to configure the {@link AdminPlugin}.
 *
 * @category Admin
 */
export declare type AdminPluginDefinition = {
    /**  the {@link Grant}'s to apply to the admin page */
    grants?: Grant[];
    /**  the {@link UserDefinition}'s to add to the application */
    users?: UserDefinition[];
    /**  the {@link EnvironmentDefinition}'s to add to the application */
    environments?: EnvironmentDefinition[];
};
export declare const UsageTable: import("../data").Table;
/**
 * Create a {@link ELARASchema} containing a {@link AdminPluginDefinition} contents.
 *
 * @param definition: the desired {@link AdminPluginDefinition}.
 * @return a {@link ELARASchema} containing a {@link AdminPluginDefinition} contents
 *
 * @category Admin
 *
 * @example
 *  ```typescript
 *  //To create one user and multiple environment variables
 *  AdminPlugin({
 *      users: [
 *          User({
 *              email: 'admin@example.com',
 *              name: 'Admin',
 *              password: ELARA.Const('admin'),
 *              roles: [],
 *              is_disabled: false,
 *              is_superuser: true,
 *              is_admin: true
 *          })
 *      ],
 *      environments: [
 *          EnvironmentVariable({ name: 'ENV_1' }),
 *          EnvironmentVariable({ name: 'ENV_2' }),
 *      ]
 *  })
 * ```
 */
export declare function AdminPlugin(definition?: AdminPluginDefinition): ELARASchema;
