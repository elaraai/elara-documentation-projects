// © Copyright 2018- 2022 - Elara AI Pty Ltd ACN 627 124 903
//
// Licensed under the EDK Licence version LICENSE-1.04.
//
// You may only use this file in accordance with the terms and conditions contained in EDK
// Licence - Version LICENSE-1.04. The full version of the EDK Licence is available at:
// https://elaraai.github.io/docs/license

import { TimeSpan } from "../east";
import { Cache } from "../datasource";
import { ELARASchema } from "../schema";
/**
 * The environment variables to define an {@link MailchimpPluginDefinition}.
 *
 * @category DataSource
 */
export declare type MailchimpEnv = {
    MAILCHIMP_KEY?: string;
};
/**
 * A {@link MailchimpPluginDefinition} to configure the {@link MailchimpPlugin}.
 *
 * @category DataSource
 */
export declare type MailchimpPluginDefinition = {
    /** the configuration of the DataSource {@link Poll} */
    poll?: TimeSpan;
    /**  the configuration of the {@link Cache} for the DataSource. */
    cache?: Cache;
    /** the environment variables to apply to the {@link MailchimpPlugin} */
    env?: MailchimpEnv;
};
/**
 * Create a streaming {@link DataSourceDefinition} for the Mailchimp Marketing API with supporting diagnostic pages: {@link https://mailchimp.com/developer/marketing/api/}.
 *
 * @param definition: the desired {@link MailchimpPluginDefinition}.
 * @return a {@link ELARASchema} containing a {@link MailchimpPluginDefinition} contents
 *
 * @category DataSource
 *
 * @example
 * ```typescript
 *   // Create a `MailchimpPlugin` request the data from the API every `hour` with default environment names.
 *   const mailchimp = MailchimpPlugin({
 *       poll: TimeSpan(1),
 *   })
 * ```
 */
export declare function MailchimpPlugin(args?: MailchimpPluginDefinition): ELARASchema;
