// © Copyright 2018- 2022 - Elara AI Pty Ltd ACN 627 124 903
//
// Licensed under the EDK Licence version LICENSE-1.04.
//
// You may only use this file in accordance with the terms and conditions contained in EDK
// Licence - Version LICENSE-1.04. The full version of the EDK Licence is available at:
// https://elaraai.github.io/docs/license

import { EnvironmentDefinition, UserDefinition } from '../admin';
import { ApplicationItemDefinition, ApplicationStyle } from '../page/Application';
import { ELARASchema } from '../schema/ELARASchema';
/**
 * A {@link ApplicationPluginDefinition} to configure the {@link ApplicationPlugin}.
 *
 * @category Application
 */
export declare type ApplicationPluginDefinition = {
    /**  the name of the {@link Application} */
    name: string;
    /**  the {@link ApplicationItemDefinition}s to inject into the application */
    items?: ApplicationItemDefinition[];
    /**  the {@link ELARASchema}s containing content groups */
    schemas?: Record<string, ELARASchema>;
    /**  set to false to prevent the input schema's from being injected */
    inject_schemas?: boolean;
    /**  the {@link ApplicationStyle} to apply */
    style?: ApplicationStyle;
    /**  the {@link UserDefinition}'s to add to the application */
    users?: UserDefinition[];
    /**  the {@link EnvironmentDefinition}'s to add to the application */
    environments?: EnvironmentDefinition[];
};
/**
 * Create an inline {@link ApplicationDefinition} based on some contents {@link ELARASchema}, for example other plugins.
 *
 * @param definition: the desired {@link ApplicationPluginDefinition}.
 * @return a {@link ELARASchema} containing a {@link ApplicationPluginDefinition} contents
 *
 * @category Application
 *
 * @example
 * ```typescript
 *   //Create a the logs and pages.
 *   const application = ApplicationPlugin({
 *       name: 'Weather & Holidays',
 *       schemas: {
 *          Weather: WeatherPlugin(),
 *          Holidays: HolidaysPlugin(),
 *       ),
 *       users: [
 *           User({
 *               email: 'admin@example.com'
 *               name: 'Admin',
 *               password: Environment('ADMIN_PASSWORD'),
 *               is_disabled: false,
 *               is_admin: true
 *           })
 *       ],
 *       env: [
 *           EnvironmentVariable({ name: 'ADMIN_PASSWORD' })
 *       ]
 *   })
 * ```
 */
export declare function ApplicationPlugin(config: ApplicationPluginDefinition): ELARASchema;
