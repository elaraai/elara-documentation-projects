// © Copyright 2018- 2022 - Elara AI Pty Ltd ACN 627 124 903
//
// Licensed under the EDK Licence version LICENSE-1.04.
//
// You may only use this file in accordance with the terms and conditions contained in EDK
// Licence - Version LICENSE-1.04. The full version of the EDK Licence is available at:
// https://elaraai.github.io/docs/license

import { ELARASchema } from "../schema/ELARASchema";
import { View } from "../analytics/View";
/**
 * A {@link ViewsPluginDefinition} to configure the {@link ViewsPlugin}.
 *
 * @category Analytics
 */
export declare type ViewsPluginDefinition = {
    /** the `Record` containing one or more {@link View}'s */
    views: Record<string, View>;
    /** prepend the artefact names with this */
    prepend?: string;
};
/**
 * Create a diagnostic page showing a the values within one ore more `View`'s
 *
 * @param definition: the desired {@link ViewsPluginDefinition}.
 * @return a {@link ELARASchema} containing a {@link ViewsPluginDefinition} contents
 *
 * @category Analytics
 *
 * @example
 * ```typescript
 *   // Use a data source
 *   import money from "./gen/money.view"
 *   import orders from "./gen/orders.view"
 *   // Create a `ViewsPlugin` to generate two pages with output of the `Views`'s.
 *   const tables = ViewsPlugin({
 *       views: {
 *           money: money.output,
 *           orders: orders.output
 *       }
 *   })
 * ```
 */
export declare function ViewsPlugin(config: ViewsPluginDefinition): ELARASchema;
