// © Copyright 2018- 2022 - Elara AI Pty Ltd ACN 627 124 903
//
// Licensed under the EDK Licence version LICENSE-1.04.
//
// You may only use this file in accordance with the terms and conditions contained in EDK
// Licence - Version LICENSE-1.04. The full version of the EDK Licence is available at:
// https://elaraai.github.io/docs/license

import { TimeSpan } from "../east";
import { Cache } from "../datasource";
import { ELARASchema } from "../schema";
/**
 * The environment variables to define an `ShopifyPlugin`.
 *
 * @category DataSource
 */
export declare type ShopifyEnv = {
    SHOPIFY_API_KEY?: string;
    SHOPIFY_PASSWORD?: string;
    SHOPIFY_URL?: string;
};
/**
 * A {@link ShopifyPluginDefinition} to configure the {@link ShopifyPlugin}.
 *
 * @category DataSource
 */
export declare type ShopifyPluginDefinition = {
    /** the configuration of the DataSource {@link Poll} */
    poll?: TimeSpan;
    /**  the configuration of the {@link Cache} for the DataSource. */
    cache?: Cache;
    /** the environment variables to apply to the {@link ShopifyPlugin} */
    env?: ShopifyEnv;
};
/**
 * Create a streaming {@link DataSourceDefinition} for the Shopify REST Admin API with supporting diagnostic pages: {@link https://shopify.dev/api/admin/rest/reference}.
 *
 * @param definition: the desired {@link MyobPluginDefinition}.
 * @return a {@link ELARASchema} containing a {@link MyobPluginDefinition} contents
 *
 * @category DataSource
 *
 * @example
 * ```typescript
 *   //Create a ShopifyPlugin and poll data from the API every `hour` with default environment names.
 *   const deputy = ShopifyPlugin({
 *       poll: TimeSpan(1),
 *   })
 * ```
 */
export declare function ShopifyPlugin(args?: ShopifyPluginDefinition): ELARASchema;
